---
name: Bug report
about: Something didn't behave as documented
labels: bug
---

**What happened**
A clear description of the bug.

**To reproduce**
Steps, ideally with the exact `worktree-harness` command(s).

**Expected**
What you expected instead.

**Environment**
- OS + version:
- `bash --version`:
- `git --version`:
- `worktree-harness --version`:
- Output of `worktree-harness doctor` (redact paths/secrets):

**Relevant `.harnessrc`** (redact secrets)
```sh
# paste here
```
